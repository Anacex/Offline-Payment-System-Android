[pytest]
# Test markers for different test types
markers =
    unit: Unit tests that run against local code
    integration: Integration tests that run against the Render production server
    slow: Tests that take a long time to run
    smoke: Smoke tests for quick validation

testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
